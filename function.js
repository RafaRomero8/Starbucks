function imgSlider(anything){
    document.querySelector('.starbucks').src = anything
    //le pasamos laa imagen en el parmetro con forme lo vallamos seleccionando
    //cambi la clase .starbucks a la imagen que seleccionamos
    //el src para que te enlace las imagenes
}


function changeCircleColor(color){
    const circle = document.querySelector('.circle')
    circle.style.background = color //para que valla cambiando de color
    //llamamos circe le damos un estilo un fondo y le pasamos el parametro color 
    //lo igulamos al color
}

